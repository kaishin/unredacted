<html>
  <head>
    <title>{{ title }} &mdash; {{ site.title }}</title>
    <meta name="description" content="{{ description }}">
  </head>
  <body>
    {{# block "main"}}
      {{{ contents }}}
    {{/ block}}
  </body>
</html>
