<html>
  <head>
    <title>
      {{# if title}}{{ title }} &mdash; {{/ if}}
      {{ site.title }}
    </title>
    <meta name="description" content="{{ description }}">
  </head>
  <body>
    {{# block "main"}}
      {{{ contents }}}
    {{/ block}}
  </body>
</html>
